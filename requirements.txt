# otherme_clipper - dependances Python
# Version cible : Python 3.11 (eviter 3.12+ pour la compatibilite mediapipe/faster-whisper)
# Installation : pip install -r requirements.txt

# --- Coeur du pipeline (MVP, phases 2 a 6) ---
faster-whisper>=1.0,<2.0     # Transcription locale avec timestamps mot par mot
yt-dlp>=2025.1               # Telechargement YouTube / Twitch VOD
numpy>=1.26,<2.0             # Calculs numeriques + profil d'energie audio du scoring
opencv-python>=4.9           # Lecture et analyse des frames video

# --- Confort et robustesse ---
pyyaml>=6.0                  # Lecture des fichiers de config
rich>=13.0                   # Affichage CLI lisible : couleurs, barres de progression
pydantic>=2.0                # Validation des configs et des donnees entre modules
python-dotenv>=1.0           # Chargement du fichier .env (cles API optionnelles)

# --- Tests ---
pytest>=8.0

# --- Phase 7 : reframe vertical ---
# Pin < 0.10.30 : les versions suivantes ont supprime l'API solutions
# (modeles embarques, zero telechargement) au profit d'une API qui exige
# de telecharger les modeles a part
mediapipe>=0.10,<0.10.30

# --- Phases ulterieures (decommenter au moment voulu) ---
# scenedetect>=0.6           # Phase 4 (option) : detection de changements de plan
# anthropic>=0.40            # Phase 10 (option) : generation titres/hashtags via API Claude
streamlit>=1.35             # Phase 14A : interface locale
